version: '3.8'

services:
  video-converter:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./videos:/app/videos
      - ./converted:/app/converted
    environment:
      - DOCKER_ENV=1
    restart: unless-stopped